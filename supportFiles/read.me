AutoStart

add "autostart" directory
     /home/{user}/.config
copy sceneplay.config 
    /home/{user}/.config/autostart
add ScenePlay.sh 
    /home/{user}/Desktop

on boot the ScenePlay will run on terminal > minimize if needed